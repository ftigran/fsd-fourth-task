//import {Event} from '../eventListenner/eventListenner'
import {IOptions, defOptions} from '../options/options'

export class Model {
    private options:IOptions;
    constructor(options: IOptions) {
      this.options=options;
    }
  }